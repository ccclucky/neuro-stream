# NeuroStream Provider — Environment Variables (v3 simplified)
# Provider is now a simple HTTP API — no wallet/escrow needed.
# Copy this file to .env and fill in your values.

# ── Server ─────────────────────────────────────────────
# Optional: Override default port
# PROVIDER_PORT=3001
