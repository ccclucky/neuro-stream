# NeuroStream Platform — Environment Variable Reference
#
# ┌─────────────────────────┬──────────┬──────────────────────────────────┐
# │ File                    │ Git      │ Contents                         │
# ├─────────────────────────┼──────────┼──────────────────────────────────┤
# │ .env                    │ Committed│ Shared non-sensitive defaults    │
# │ .env.development        │ Committed│ Dev non-sensitive (URLs, IDs)    │
# │ .env.production         │ Committed│ Prod non-sensitive (URLs, IDs)   │
# │ .env.local              │ Ignored  │ Local overrides (all envs)       │
# │ .env.development.local  │ Ignored  │ Dev secrets (keys, passwords)    │
# │ .env.production.local   │ Ignored  │ Prod secrets (keys, passwords)   │
# └─────────────────────────┴──────────┴──────────────────────────────────┘
#
# NOTE: Agent and Provider are EXTERNAL services.
#       Their env vars live in apps/agent/.env and apps/provider/.env respectively.
#       See apps/agent/.env.example and apps/provider/.env.example.

# ═══════════════════════════════════════════════════════════
# Platform variables (root .env files)
# ═══════════════════════════════════════════════════════════

# ── Blockchain ──────────────────────────────────────────────
MONAD_RPC_URL=http://127.0.0.1:8545         # Monad / Hardhat RPC endpoint
CHAIN_ID=31337                               # Chain ID (31337=Hardhat, 10143=Monad Testnet)
PAYMENT_TOKEN_DECIMALS=6                     # Token decimals (6 for USDC)
PAYMENT_TOKEN_ADDRESS=                       # ERC20 token address (fill after deploy)
ESCROW_CONTRACT_ADDRESS=                     # Escrow contract address (fill after deploy)
PLATFORM_ADDRESS=                            # Platform fee recipient (defaults to deployer)
PLATFORM_FEE_BPS=200                         # Platform fee in basis points (200 = 2%, max 5000)

# ── Supabase (public) ────────────────────────────────────
SUPABASE_PROJECT_REF=                        # Supabase project reference
SUPABASE_URL=                                # Supabase project URL
SUPABASE_ANON_KEY=                           # Supabase anonymous key (public by design)

# ── Privy (public) ───────────────────────────────────────
PRIVY_APP_ID=                                # Privy application ID

# ── Gateway ──────────────────────────────────────────────
GATEWAY_WALLET_ADDRESS=                      # Gateway wallet address (public)

# ── Next.js public vars ─────────────────────────────────
NEXT_PUBLIC_MONAD_RPC_URL=
NEXT_PUBLIC_CHAIN_ID=
NEXT_PUBLIC_PAYMENT_TOKEN_ADDRESS=
NEXT_PUBLIC_PAYMENT_TOKEN_DECIMALS=6
NEXT_PUBLIC_ESCROW_CONTRACT_ADDRESS=
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
NEXT_PUBLIC_PRIVY_APP_ID=

# ── Misc ───────────────────────────────────────────────────
REPORT_GAS=false                             # Enable Hardhat gas reporter

# ═══════════════════════════════════════════════════════════
# Platform secrets (.env.*.local only — NEVER commit!)
# ═══════════════════════════════════════════════════════════

# DEPLOYER_PRIVATE_KEY=                      # Wallet private key for contract deployment
# SUPABASE_SERVICE_ROLE_KEY=                 # Supabase service role key (admin access)
# SUPABASE_DB_URL=                           # PostgreSQL connection string
# PRIVY_APP_SECRET=                          # Privy application secret
# GATEWAY_PRIVATE_KEY=                       # Gateway wallet private key
