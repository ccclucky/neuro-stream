# NeuroStream — Universal defaults (committed to Git)
# These apply to ALL environments and can be overridden by .env.<env> files.

PAYMENT_TOKEN_DECIMALS=6
PLATFORM_FEE_BPS=200
PROVIDER_PORT=3001
REPORT_GAS=false
