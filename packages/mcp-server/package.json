{
  "name": "@neurostream/mcp-server",
  "version": "0.0.1",
  "description": "NeuroStream MCP Server — discover and pay for AI agent services with escrow delivery guarantee",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "bin": {
    "neurostream-mcp": "./dist/index.js"
  },
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "start": "node dist/index.js",
    "test": "vitest run",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.29.0",
    "zod": "^3.25.0"
  },
  "devDependencies": {
    "@types/node": "^20.11.5",
    "typescript": "^5.3.3",
    "vitest": "^1.2.2"
  }
}